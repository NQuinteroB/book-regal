
<div class="banner my-3">
  <div class="text-center" data-aos="zoom-out-up"><div>
  <h1 style="line-height: 2em;" class="my-3 mx-3 font-weight-bold border"> Welcome <%= current_user.first_name %></h1>
</div>


<div data-aos="fade-up"
  data-aos-easing="linear"
  data-aos-duration="1500">
  <div class="row">
    <div class="books-grid d-flex justify-content-around">
      <% @books1.each do |book| %>
        <%= render partial: "shared/card", locals: {book: book }  %>
      <%end%>
    </div>
  </div>
</div>

<div class="my3">
  <div data-aos="zoom-in-right">
    <h2 class="my-3 mx-3 font-weight-bold border" style="line-height: 3em;">Your rented books</h2>
  </div>
</div>

</div>
  <div data-aos="fade-up"
    data-aos-easing="linear"
    data-aos-duration="1500">
    <div class="row">
      <div class="books-grid d-flex justify-content-around">
        <% @books2.each do |book| %>
          <%= render partial: "shared/card", locals: {book: book }  %>
        <%end%>
      </div>
    </div>
  </div>
</div>
