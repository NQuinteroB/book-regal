<div class="text-center font-weight-bold">
   <h1>All Books👇</h1> 
</div>


<div class="d-flex justify-content-around mt-5">
    <ul style="list-style-type: none">
        <% @books.each do |book| %>
       
        <li style="display: inline; margin-left: 20px;">
        <% if book.photo.attached? %>
          <%= cl_image_tag book.photo.key, height: 200, width: 150, crop: :fill %>
      <button type="button" class="btn btn-light align-text-bottom">    <%= link_to book.title, book_path(book)  %> </button>
        <% else %>
          <%= book.title %>
        <% end %>
        </li>
        <%end%>
    </ul>
  </div>
